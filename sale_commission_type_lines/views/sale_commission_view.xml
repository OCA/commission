<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record model="ir.ui.view" id="sale_commission_form_inherit">
        <field name="name">Sales commissions form inherit</field>
        <field name="model">sale.commission</field>
        <field name="inherit_id" ref="sale_commission.sale_commission_form" />
        <field name="arch" type="xml">
            <form position="inside">
                <field
                    name="item_ids"
                    nolabel="1"
                    attrs="{'invisible':[('commission_type', '!=', 'cat_prod_var')]}"
                >
                    <tree string="Items">
                        <field name="name" string="Applied On" />
                        <field name="commission_type" string="Compute Price" />
                        <field name="commission_value" />
                    </tree>
                </field>
            </form>
        </field>
    </record>
</odoo>
