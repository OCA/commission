<odoo>
    <data>
        <record id="sale_commission_brand_form" model="ir.ui.view">
            <field name="name">sale.commission.brand.form</field>
            <field name="model">sale.commission</field>
            <field name="inherit_id" ref="sale_commission.sale_commission_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@colspan='4']" position="after">
                    <field name="brand_lines" widget="one2many_list"
                           colspan="4"
                           nolabel="1"
                            attrs="{'invisible': [('commission_type', '!=', 'brand')]}">
                        <tree string="brand_lines" editable="bottom">
                            <field name="brand_id"/>
                            <field name="percent"/>
                        </tree>
                    </field>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
